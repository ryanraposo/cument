#!/usr/bin/python3
import sys
from pathlib import Path

checkout = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(checkout if (checkout / 'cument').is_dir() else Path('/usr/lib/cument')))
from cument.cli import main
raise SystemExit(main())
